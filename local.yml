---

- hosts: localhost
  pre_tasks:
    - name: Update packages
      become: true
      community.general.pacman:
        update_cache: true
        upgrade: true
      tags:
        - install
        - core

  # -----------------------------------------------------------------------

- hosts: localhost
  tags: base
  become: true
  roles:
    - base

  # -----------------------------------------------------------------------

