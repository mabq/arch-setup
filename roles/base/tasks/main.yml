# Why this way?
#   It's better to group tasks by what part of the system they affect

# user setup
- import_tasks: tasks/git.yml

# install software
- import_tasks: software/pacman.yml
- import_tasks: software/install.yml

# system setup
#   some tasks require the files to be in place
- import_tasks: system_setup/clock.yml
- import_tasks: system_setup/ssh.yml

# - zsh (before user for default shell)
# - user (adjustments only, the home folder should already be there)
# - tmux
# - nvim
#   - many things here...
# - node

