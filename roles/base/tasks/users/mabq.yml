- tags: always
  include_vars: users/mabq.yml

- name: hello
  ansible.builtin.debug:
    msg: "hello"

# --- User account ---

# To do:
#   - change shell to zsh
#
# - name: Create user account for "mabq"
#   tags: users,users_mabq,users_mabq_create
#   block:
#
#   - name: Create user group
#     ansible.builtin.group:
#       name: mabq
#
#   - name: Generate password hash
#     # arch changes the password hashing algorithm from `sha512` to `yescript` (https://archlinux.org/news/changes-to-default-password-hashing-algorithm-and-umask-settings/)
#     # openssl does not support `yescript` yet, when it does update the `-6` flag to the one corresponding to `yescript`
#     ansible.builtin.command: "openssl passwd -6 {{ password }}"
#     register: password_hash
#
#   - name: Create user account
#     ansible.builtin.user:
#       name: mabq
#       # primary group
#       group: mabq
#       # supplementary groups --- https://wiki.archlinux.org/title/Users_and_groups#Group_list
#       # (`input` and `uinput` are required for `kmonad`)
#       # TODO: add input and uinput groups to mabq in the kmonad section, uinput does not exist during the live environment
#       groups: adm,wheel,input,uinput
#       # password hashed are stored in `/etc/shadow`
#       password: "{{ password_hash.stdout }}"
#       # the login shell must be one of those listed in `/etc/shells` (zsh is not yet installed)
#       shell: /bin/bash
#
#   - name: Create sudoers file
#     ansible.builtin.copy:
#       src: users/mabq/sudoers/mabq
#       # https://wiki.archlinux.org/title/Sudo#Configure_sudo_using_drop-in_files_in_/etc/sudoers.d
#       dest: /etc/sudoers.d/
#       owner: root
#       group: root
#       mode: 0440
#
# # ------------------------------------------------------------------------------
#
# - tags: mabq_dotfiles
#   block:
#
#   - name: base | users | mabq | template dotfiles
#     # use the template module to share a single location for all src files
#     ansible.builtin.template:
#       src: "users/mabq/{{ item.src }}"
#       dest: "/home/mabq/{{ item.dest }}"
#       owner: mabq
#       group: mabq
#       mode: "{{ item.mode | default('0600') }}"
#     with_items:
#       # use directories as `dest`, the same `src` filename will be used
#       - { src: 'git/.gitconfig', dest: '.', mode: '0644' }
#       - { src: 'ssh/id_ed25519', dest: '.ssh/' }
#       - { src: 'ssh/id_ed25519.pub', dest: '.ssh/', mode: '0640' }
#       - { src: 'tmux/tmux.conf', dest: '.config/tmux/' }
#       - { src: 'tmux/tmux-sessionizer.sh', dest: '.config/tmux/scripts/', mode: '0700' }
#       # - { src: 'zsh/.zshenv',                 dest: '.zshenv' }
#       # - { src: 'zsh/.zshrc',                  dest: '.config/zsh/.zshrc' }
#       # - { src: 'zsh/bindkey.zsh',             dest: '.config/zsh/bindkey.zsh' }
#       # - { src: 'zsh/functions.zsh',           dest: '.config/zsh/functions.zsh' }
#       # - { src: 'zsh/options.zsh',             dest: '.config/zsh/options.zsh' }
#       # - { src: 'zsh/plugins.zsh',             dest: '.config/zsh/plugins.zsh' }
#       # - { src: 'joshuto/joshuto.toml',        dest: '.config/joshuto/joshuto.toml' }
#       # - { src: 'joshuto/keymap.toml',         dest: '.config/joshuto/keymap.toml' }
#       # - { src: 'joshuto/mimetype.toml',       dest: '.config/joshuto/mimetype.toml' }
#       # - { src: 'joshuto/theme.toml',          dest: '.config/joshuto/theme.toml' }
#       # - { src: 'joshuto/icons.toml',          dest: '.config/joshuto/icons.toml' }
#       # - { src: 'joshuto/preview_file.sh',     dest: '.config/joshuto/preview_file.sh',       mode: '0700'}
#

