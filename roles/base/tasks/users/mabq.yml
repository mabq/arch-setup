- name: Setup mabq
  tags: mabq
  block:

  # ---------------------------------------------------------------------------
  
  - name: Import variables - mabq
    include_vars: users/mabq.yml

  # ---------------------------------------------------------------------------

  - name: Create the user - {{ name }}
    block:

    - name: Create the group - {{ name }}
      ansible.builtin.group:
        name: "{{ name }}"

    - name: Create the user - {{ name }}
      block:

      - name: Generate password hash
        ansible.builtin.command: "openssl passwd -6 {{ password }}"
        register: password_hash

      - name: Create user - {{ name }}
        ansible.builtin.user:
          name: "{{ name }}"
          group: "{{ name }}"
          # - adm: commonly used to give read access to protected logs. It has full read access to journal files
          # - wheel: commonly used to give privileges to perform administrative actions
          groups: adm,wheel
          # see `/etc/shadow`
          password: "{{ password_hash.stdout }}"
          shell: /bin/bash

    - name: Setup privilege elevation - {{ name }}
      #  https://wiki.archlinux.org/title/List_of_applications/Security#Privilege_elevation
      ansible.builtin.copy:
        src: users/{{ name }}/sudoers
        dest: /etc/sudoers.d/{{ name }}
        owner: root
        group: root
        mode: 0440

  # ---------------------------------------------------------------------------
  
  - name: Setup ssh
    block:

    - name: Ensure .ssh directory exists - {{ name }}
      ansible.builtin.file:
        path: "/home/{{ name }}/.ssh"
        state: directory
        owner: "{{ name }}"
        group: "{{ name }}"
        mode: 0700

    - name: Install private ssh key - {{ name }}
      ansible.builtin.copy:
        src: "users/{{ name }}/ssh/{{ name }}"
        dest: "/home/{{ name }}/.ssh/{{ name }}"
        owner: "{{ name }}"
        group: "{{ name }}"
        mode: 0600

    - name: Install public ssh key - {{ name }}
      ansible.builtin.copy:
        src: "users/{{ name }}/ssh/{{ name }}.pub"
        dest: "/home/{{ name }}/.ssh/{{ name }}.pub"
        owner: "{{ name }}"
        group: "{{ name }}"
        mode: 0644

    - name: Add the public key to the `authorized_key` file
      # one key to rule them all
      ansible.posix.authorized_key:
        user: "{{ name }}"
        key: "{{ item }}"
      with_file:
        - "users/{{ name }}/ssh/{{ name }}.pub"

    # - becasue I use a single key there is no need to create a config file. The default value of `AddKeysToAgent` is `yes`.

  # ---------------------------------------------------------------------------
